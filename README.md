## Homepage for the band aaltos

This project is a static website showcasing the band aaltos. The project uses Javascript, React, CSS and HTML.

To continue developing the project in a new environment:

```properties
git clone [repo-url]
cd [project-folder]
npm install  
npm run dev  
``` 

